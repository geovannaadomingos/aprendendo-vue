import { createApp } from 'vue'
import App from './App.vue'

createApp(App).mount('#app')

// O createApp é a função do vue que vai possibilitar a manipulação da nossa página html por meio de vue. neste caso, há uam função mount que vai montar no html com id "app" o App.vue importado e criado pela função do vue createApp 